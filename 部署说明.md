# 云端构思板：Netlify + Supabase 部署交接

这是可独立部署到 Netlify 的交付包。任务数据、账号和私有截图都由 Supabase 承载；DeepSeek 与 Supabase 管理密钥只保存在 Netlify 服务端环境变量中，不会进入浏览器构建。

## 全新项目：一次性初始化 Supabase

1. 新建一个 Supabase 项目，打开 **SQL Editor**。
2. 将 `supabase/schema.sql` 的完整内容粘贴并执行一次。
3. 在 **Authentication → URL Configuration** 中添加你的 Netlify 正式地址和预览地址为 Redirect URL。
4. 在 **Project Settings → API** 复制 Project URL 与 `anon public` key。不要使用或上传 `service_role` key。

## 已有项目：无损升级

已有数据的站点不要重新执行完整的 `schema.sql`。在 Supabase **SQL Editor** 中执行：

`supabase/migrations/20260824_memo_flow.sql`

`supabase/migrations/20260824_restore_ai_organizer.sql`

两个迁移均可重复执行：分别补齐完成/截图流程和 AI 原文/整理稿字段，不会删除现有卡片或截图。必须先完成数据库迁移，再部署新版前端。

执行前备份并核对数据库现状。完成/截图迁移仅在首次新增 `sort_order` 列时初始化排序；已存在该列时保留全部原值（包括已有相同值），不会猜测或重排。修订版无法还原旧脚本已经覆盖的顺序。

## 配置 Netlify

在 Netlify 的 **Environment variables** 中添加：

| 变量 | 值 |
| --- | --- |
| `VITE_SUPABASE_URL` | Supabase Project URL |
| `VITE_SUPABASE_ANON_KEY` | Supabase publishable / anon public key |
| `SUPABASE_URL` | Supabase Project URL，仅供 Netlify Function 使用 |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase service role key，仅限 Netlify 服务端作用域 |
| `DEEPSEEK_API_KEY` | DeepSeek API key，仅限 Netlify 服务端作用域 |
| `DEEPSEEK_MODEL` | 填写你的 DeepSeek 账号可用模型；未设置时使用代码默认值 `deepseek-chat` |

密钥变量必须同时覆盖需要使用 AI 的 production/branch-deploy 上下文。随后重新部署即可。构建命令、发布目录和函数目录已在 `netlify.toml` 中配置。

## 数据与权限

- `cards` 和 `attachments` 均启用了行级权限，每个登录邮箱只能读取、修改和删除自己的记录。
- `screenshots` 存储桶为私有，图片路径按用户 ID 分隔，并使用 1 小时的临时访问链接。
- 登录采用邮箱和密码；支持忘记密码邮件与重设密码页面。
- AI Function 会先验证 Supabase 登录令牌，再把当前卡片的 `originalText` 发送给 DeepSeek；账号、其他卡片和截图不会进入提示词。
- AI 结果必须先预览并由用户采用；服务端限制原文长度、请求频率和响应格式，所有响应禁止缓存。
- 完成卡片后有 6 秒撤销入口，恢复时回到完成前的阶段。
- 档案按今天、昨天、本周、更早分组，可按文字、编号或截图文件名搜索并恢复。
- 浏览器会缓存最近一次成功数据和未提交文字草稿；同步状态会区分同步中、已同步、离线和失败。
- 手机端只编辑文字、切换阶段和查看截图；上传、下载、删除和排序截图仅在桌面端显示。
- 截图仅接受 JPG、PNG、WebP，单张上限 10 MB。
- 图片访问链接按需生成；单张加载失败只显示该图片的重试入口，不影响整张卡片。

## 发布顺序与验收

1. 已有项目先执行增量迁移。
2. 运行 `npm run lint`、`npx tsc --noEmit` 和 `npm test`。
3. 将 `dist` 目录作为 Netlify 发布目录部署。
4. 在正式站依次验证：登录、AI 梳理与采用、创建文字、首次创建时粘贴截图、跨阶段移动、完成与撤销、档案恢复、刷新后同步、手机端查看截图。

不要把 `.env.production`、数据库密码或任何 secret/service-role key 上传到仓库或粘贴到前端代码中。
